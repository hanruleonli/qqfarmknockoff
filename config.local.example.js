// Copy this file to `config.local.js` (which is gitignored) and edit secrets locally.
// Example: set a plain-text admin password and optional adminId.

window.ADMIN_CONFIG = {
  // Plain admin password — the app will hash this on startup.
  adminPassword: "128560",
  // Optional: set which player id is the admin (e.g., "p1234").
  // adminId: "p..."
};
